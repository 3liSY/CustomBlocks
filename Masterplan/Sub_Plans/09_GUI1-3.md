# GUI Navigation

# Active Batch: G1

*This batch was moved from the main backlog. View the full backlog here: [MASTERPLAN.md](../MASTERPLAN.md)*

---

## 1. G1: Back Buttons Go to Main Menu Instead of Previous Screen

[x] **Code Written**
[x] **Tested In-Game** — confirmed 2026-06-02

**State:** 🔴 BROKEN — 2 Back buttons incorrectly call `openMain(player, 0)` instead of `handleEscBack(player)`. All other back buttons in the file are already correct.

**Files:** `gui/GuiManager.java`

**Technical Details:**
* Audit confirmed only 2 violations remain (masterplan line numbers were stale from an older code version).
* **Violation 1 — Line 1368** (WELCOME_MENU click handler): `else if (slot == 49) { openMain(player, 0); }` — the Back button in the welcome/onboarding screen goes to main menu instead of the previous screen.
* **Violation 2 — Line 2990** (UNDO_PICKER click handler): `if (slot == 45) { openMain(player, 0); return; }` — the Back button in the undo history picker goes to main menu instead of the previous screen.
* `handleEscBack(player)` is the correct call — it pops the GUI back-stack and restores the previous screen.

**The Fix:**
* Line 1368: change `openMain(player, 0)` → `handleEscBack(player)`
* Line 2990: change `openMain(player, 0)` → `handleEscBack(player)`

**Test Plan:**
1. Open the welcome/onboarding screen and click Back — should return to previous screen, not main menu.
2. Open `/cb undogui`, click Back — should return to previous screen, not main menu.



### G3 — 10 Missing Cases in restoreState()
**State:** ✅ CONFIRMED IN-GAME (2026-06-02) — Back/ESC working on all 10 screens.
**File:** `gui/GuiManager.java` (`restoreState()`)
**Priority:** 🟠

These GuiMode values exist in `GuiMode.java` but were missing from the `restoreState()` switch: `BULK_ASSIGN_PICKER`, `BULK_RECOLOR_CONFIRM`, `BULK_RECOLOR_WIZARD`, `CATEGORY_BLOCK_CONTEXT`, `CATEGORY_ICON_PICKER`, `CATEGORY_STATS`, `DELETE_CATEGORY_MENU`, `IMPORT_CONFLICT`, `MERGE_CATEGORY_PICKER_TARGET`, `SORT_BLOCKS_MENU`.

For each: find the corresponding `open*()` method, read its real parameter signature, then add the case using parameters from `state`. Do NOT copy signatures from this plan — verify each by reading the method.

**Test:** Enter each of the 10 screens, press ESC. Should return to the previous screen, not main menu.
